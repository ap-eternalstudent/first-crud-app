<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <link rel="stylesheet" href="style.css" />
    <title>My CRUD App</title>
  </head>
  <body>
    <h1 data-position="header">May Node and Express be with you</h1>
    <section data-position="add-quote">
      <h2>Add Quote</h2>
      <form action="/quotes" method="POST">
        <input type="text" placeholder="name" name="name" />
        <input type="text" placeholder="quote" name="quote" />
        <button type="submit">Submit</button>
      </form>
    </section>
    <section data-position="update-quote">
      <h2>Darth Vader invades!</h2>
      <div>
        <ul class="quotes">
          <span id="messageDiv">No Vader quotes to delete</span>
          <% for(var i = 0; i < quotes.length; i++) {%>
          <li class="quote">
            <span><%= quotes[i].name %></span>:
            <span><%= quotes[i].quote %></span>
          </li>
          <% } %>
        </ul>
      </div>
    </section>
    <section data-position="delete-quote">
      <div>
        <h2>Darth Vader invades!</h2>
        <p>Replace first Yodas quote with a quote written by Darth Vadar</p>
        <button id="update-button">Replace Yodas quote</button>
      </div>
    </section>
    <div>
      <h2>Remove Darth Vadar!</h2>
      <p>
        Delete one Darth Vaders quote. Does nothing if there are no more Darth
        Vaders quotes
      </p>
      <button id="delete-button">Delete Darth Vaders quote</button>
    </div>
  </body>
  <script src="/main.js"></script>
</html>
